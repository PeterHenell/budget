# Test Environment Configuration
# This file contains environment variables for integration testing
# Uses separate database and ports to avoid conflicts with development

# Test Database Configuration
POSTGRES_HOST=localhost
POSTGRES_DB=budget_test_db
POSTGRES_USER=budget_test_user
POSTGRES_PASSWORD=budget_test_password
POSTGRES_PORT=5433

# Test Application Configuration
FLASK_SECRET_KEY=test-secret-key-for-integration-tests-only
FLASK_PORT=5001
FLASK_DEBUG=False

# Test-specific settings
TEST_MODE=true
